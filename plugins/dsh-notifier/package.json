{
  "name": "@baobaolaodie/cc-dsh-notifier",
  "version": "0.1.3",
  "description": "cc-dsh-notifier dsh plugin: forwards dsh session events to the shared daemon for Windows toast notifications (web + tui profiles; Claude Code side installs via the repository installer)",
  "type": "module",
  "main": "lib/index.js",
  "files": [
    "lib",
    "cordis.patch.yml",
    "README.md"
  ],
  "dsh": {
    "bundle": {
      "patch": "cordis.patch.yml"
    }
  }
}
